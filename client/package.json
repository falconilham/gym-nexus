{
  "name": "@fitflow/client",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "dev": "cp .env.local .env && expo start",
    "dev:local": "cp .env.local .env && expo start",
    "dev:prod": "cp .env.production .env && expo start",
    "android": "expo start --android",
    "android:local": "cp .env.local .env && expo start --android",
    "android:prod": "cp .env.production .env && expo start --android",
    "ios": "expo start --ios",
    "ios:local": "cp .env.local .env && expo start --ios",
    "ios:prod": "cp .env.production .env && expo start --ios",
    "web": "expo start --web",
    "clean": "rm -rf node_modules .expo"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.0.3",
    "@react-native-async-storage/async-storage": "2.2.0",
    "axios": "1.13.2",
    "expo": "~54.0.30",
    "expo-image-picker": "17.0.10",
    "expo-linear-gradient": "~15.0.8",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-svg": "15.12.1"
  },
  "private": true,
  "devDependencies": {
    "@types/react": "~19.1.10",
    "typescript": "~5.9.2"
  }
}
